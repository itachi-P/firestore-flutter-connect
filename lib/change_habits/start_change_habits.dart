import 'package:flutter/material.dart';

class ChangeHabits extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Column(
      children: <Widget>[
        Text('（仮）'),
      ],
    );
  }
}
